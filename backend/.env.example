# ===========================================
# BACKEND ENVIRONMENT CONFIGURATION
# ===========================================
# Copiar este archivo como .env y configurar los valores

# --- Servidor ---
NODE_ENV=development  # development | staging | production
PORT=3000

# --- Base de Datos ---
DB_HOST=localhost     # En Docker: postgres
DB_PORT=5432
DB_NAME=dental_db
DB_USER=postgres
DB_PASSWORD=tu_password_seguro

# --- Autenticación ---
# IMPORTANTE: Cambiar en producción por una clave larga y aleatoria
JWT_SECRET=cambiar_por_clave_segura_de_al_menos_32_caracteres

# --- CORS (Cross-Origin Resource Sharing) ---
# Lista de orígenes permitidos separados por coma
# En desarrollo:
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# En producción (ejemplo):
# CORS_ALLOWED_ORIGINS=https://mi-clinica.com,https://admin.mi-clinica.com

# --- Debug (solo desarrollo) ---
DEBUG_CORS=false  # true para ver logs de CORS en consola
